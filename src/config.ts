import type { Config } from './types/globals.js';

export const config: Readonly<Config> = {
  paths: {
    data: '/data',
    beTemplate1: 'templates/BLD-BE-001 Budget Estimate template.xlsx',
    beTemplate2:
      'templates/BE-002 Budget Estimate template for monitoring.xlsx',
  },
};
